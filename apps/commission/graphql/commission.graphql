type Commission {
    id: ID!
    commission_type: String!
    vc_name: String!
    vehicle: Vehicle
    component: Component
    contractor: Contractor
    sale_invoices: [SaleInvoice]!
    description: String
    status: String!
    start_date: String! 
    end_date: String
    upload: Boolean!
}

type CommissionConnection {
    pageInfo: PageInfo!
    objects: [Commission]!
}

input CommissionFilter {
    status: CommissionStatus
    id: ID
    vc_name__icontains: String
}

enum CommissionStatus{
    OPEN
    READY
    DONE
    CANCELLED
    ON_HOLD
}

enum CommissionType{
    VEHICLE
    COMPONENT
}

extend type Query {
    commissions(pagination: PageInput, filters: CommissionFilter): CommissionConnection!
}

extend type Mutation {
    updateStatus(id: ID!, status: CommissionStatus!): Commission!
}
