name: django-klima-kar

on: [push]

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python 3.7
      uses: actions/setup-python@v1
      with:
        python-version: 3.7
    - name: Setup PostgreSQL
      uses: Harmon758/postgresql-action@v1.0.0
      with:
        postgresql db: klimakar
        postgresql user: user 
        postgresql password: password
    - name: Install system dependecies
      run: |
        sudo apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info libpq-dev postgresql postgresql-contrib
    - name: Setup Redis
      uses: zhulik/redis-action@v1.0.0
    - name: Install fonts
      run: |
        sudo cp KlimaKar/static/fonts/* /usr/local/share/fonts/
        sudo fc-cache -fv
    - name: Compile aztec code decoder
      run: |
        g++ -o scripts/aztec scripts/aztec.cpp
    - name: Copy local settings
      run: |
        cp docs/settings_local.github.py KlimaKar/settings_local.py
    - name: Install python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r docs/requirements.pip
    - name: Lint with flake8
      run: |
        pip install flake8
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=128 --statistics
    #- name: Test with pytest
    #  run: |
    #    pip install pytest
    #    pytest
