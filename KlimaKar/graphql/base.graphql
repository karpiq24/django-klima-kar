type Query {
    _unused: Boolean
}

type Mutation {
    _unused: Boolean
}

type PageInfo {
    hasNextPage: Boolean!
    hasPreviousPage: Boolean!
    count: Int!
    numPages: Int!
    pageNumber: Int!
}

input PageInput {
    page: Int
    pageSize: Int
}
